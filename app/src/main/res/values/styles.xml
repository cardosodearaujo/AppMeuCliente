<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="StyledTilEditTextTheme">
        <item name="colorControlNormal">#e6ffffff</item> // Cor padrão da linha
        <item name="colorControlActivated">#faffffff</item> // Cor linha e texto quando recebe foco
    </style>

    <style name="StyledTilEditText">
        <item name="android:theme">@style/StyledTilEditTextTheme</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:textColorHint">#e6ffffff</item> // Cor texto(hint) sem foco
    </style>

    <style name="StyledTilEditTextThemeClean">
        <item name="colorControlNormal">@android:color/transparent</item> // Cor padrão da linha
        <item name="colorControlActivated">@android:color/transparent</item> // Cor linha e texto quando recebe foco
    </style>

    <style name="StyledTilEditTextClean">
        <item name="android:theme">@style/StyledTilEditTextThemeClean</item>
    </style>

</resources>
